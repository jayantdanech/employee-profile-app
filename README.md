# project_app_employee

```
employee-profile-app/
├── app.py                  # Main Flask application file
├── requirements.txt        # Python package dependencies
├── schema.sql              # SQL to create the database table
├── static/                 # Folder for static files (CSS, JS, saved images)
│   └── uploads/            # Location to store uploaded photos
└── templates/              # Folder for HTML templates
    ├── main.html           # Main page (New/Existing Employee input)
    └── info.html           # Employee information display
```


